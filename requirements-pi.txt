# Extra dependencies needed only on the Raspberry Pi itself (shake sensor +
# SPI screen). Install after requirements.txt: pip install -r requirements-pi.txt
smbus2>=0.4
RPi.GPIO>=0.7.1
luma.lcd>=2.11
